<nav class="navbar navbar-light bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand text-white">Home</a>
    <form class="d-flex">
   <a href="home/logout" class="logout text-secondary"><button type="button"  class="btn btn-light ">  Logout</button></a>
    
    </form>
  </div>
</nav>

<section>
  <div class="container mt-5 ">
    <div class="row">
    
      {{#each product}}
  <div class="col-xl-3 col-lg-3 col-md-6 col-sm-6">
  <div class="card border border-dark mt-4" style="   ">
  <img src="{{this.image}}" class="card-img-top images " style="height: 250px; width: 150px" alt="..." >
  <div class="card-body">
    {{!-- <img src="{{this.image}}" alt=""> --}}
    <h5 class="card-title">{{this.title}}</h5>
    <p class="card-text">{{this.description}}</p>
    <p class="card-text">{{this.Price}}</p>
    <p class="card-text">{{this.EMIOption}}</p>
    <a href="#" class="btn btn-primary">Add to cart</a>
  </div>
</div>
</div>

{{/each}}

</div>
</div>
</section>

